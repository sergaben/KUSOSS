
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                        controllers.HomeController.index
# Trying connection between backend and frontend
POST    /registerKStudents       controllers.RegistrationKSController.registerKStudents
#GET    /connectionBetweenBackendFrontend       controllers.ChatClientController.connectionBetweenBackendFrontend
GET     /getCourses           controllers.getCoursesController.getCourses
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

      