
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                        controllers.HomeController.index
# Trying connection register kstudents
POST    /registerKStudents       controllers.RegistrationKSController.registerKStudents
#Getting courses from databases and converting them into scala objects
GET     /getCourses           controllers.getCoursesController.getCourses
#Getting subjects from database and converting them into scala objects
GET     /getSubjectsNames      controllers.getSubjectsController.getSubjectsNames
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

      